# Required for NextAuth. Generate: `openssl rand -base64 32`
AUTH_SECRET=****

# SQLite path. Default: ./data/yak.sqlite
# DATABASE_PATH=./data/yak.sqlite

# --- Groq (primary chat + memory summarization) ---
GROQ_API_KEY=****

# --- CLIProxyAPI (coding tasks: Codex primary, Claude secondary, Gemini tertiary) ---
CLIPROXY_BASE_URL=http://localhost:8317/v1
AI_PROXY_KEY=fake

# --- Optional: file uploads (S3-compatible e.g. Garage) ---
# S3_ENDPOINT=http://localhost:3900
# S3_REGION=garage
# S3_BUCKET=yak-files
# S3_ACCESS_KEY_ID=****
# S3_SECRET_ACCESS_KEY=****
# S3_PUBLIC_URL=https://uploads.example.com

# --- Jobs (background cron via Supercronic) ---
# JOB_TOKEN=<generate: openssl rand -base64 32>

# --- Production (Docker) ---
# NEXTAUTH_URL=https://chat.yourdomain.com
# CLOUDFLARE_TUNNEL_TOKEN=****
